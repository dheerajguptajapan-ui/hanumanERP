import { db } from '../db';

/**
 * Sample data seeding has been intentionally removed.
 * The application starts fresh with no pre-loaded data.
 * Users configure their business via the First-Run Setup Wizard.
 */
export async function seedSampleData(): Promise<boolean> {
  // No-op: all sample/demo data removed.
  return false;
}
